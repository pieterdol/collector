/** Placeholder shell — real routing and pages arrive with the frontend step. */
export default function App() {
  return (
    <div className="grid min-h-screen place-items-center">
      <p className="font-mono text-sm text-[var(--muted)]">Collector — scaffold OK</p>
    </div>
  );
}
